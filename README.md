# ID Card Recognition App

Cette application Streamlit détecte et lit les textes sur les cartes d'identité, passeports, etc.

## Lancer en local

```bash
pip install -r requirements.txt
streamlit run app.py
